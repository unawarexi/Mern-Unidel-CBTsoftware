import debounce from "lodash/debounce";
import throttle from "lodash/throttle";

// Export only the two core utilities for UI and API usage
export { debounce, throttle };

export default { debounce, throttle };
